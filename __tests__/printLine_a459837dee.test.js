// ********RoostGPT********
/*
Test generated by RoostGPT for test testM using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=printLine_adf0a41ef1
ROOST_METHOD_SIG_HASH=printLine_a459837dee


*/

// ********RoostGPT********
const assert = require('assert');
const sinon = require('sinon');
const colors = require('colors');
const app = require('../weather/app');

describe('printLine', function() {
  let consoleLog;

  beforeEach(function() {
    consoleLog = sinon.stub(console, 'log');
  });

  afterEach(function() {
    consoleLog.restore();
  });

  it('should print text in green when required is true', function() {
    const text = 'Test text';
    app.printLine(true, text);
    assert(consoleLog.calledWith((String.fromCharCode(10004) + " " + text).green));
  });

  it('should print text in red when required is false', function() {
    const text = 'Test text';
    app.printLine(false, text);
    assert(consoleLog.calledWith((String.fromCharCode(10006) + " " + text).red));
  });

  it('should throw an error when text is not provided', function() {
    assert.throws(() => app.printLine(true), Error);
  });
});
