// ********RoostGPT********
/*
Test generated by RoostGPT for test testM using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=printLine_adf0a41ef1
ROOST_METHOD_SIG_HASH=printLine_a459837dee


*/

// ********RoostGPT********
const chai = require('chai');
const expect = chai.expect;
const sinon = require('sinon');
const app = require('./app');

describe('printLine method', () => {
  let consoleLogStub;

  beforeEach(() => {
    consoleLogStub = sinon.stub(console, 'log');
  });

  afterEach(() => {
    consoleLogStub.restore();
  });

  it('should print text in green when required is true', () => {
    const required = true;
    const text = 'Test text';

    app.printLine(required, text);

    expect(consoleLogStub.calledOnce).to.be.true;
    expect(consoleLogStub.calledWith((String.fromCharCode(10004) + " " + text).green)).to.be.true;
  });

  it('should print text in red when required is false', () => {
    const required = false;
    const text = 'Test text';

    app.printLine(required, text);

    expect(consoleLogStub.calledOnce).to.be.true;
    expect(consoleLogStub.calledWith((String.fromCharCode(10006) + " " + text).red)).to.be.true;
  });
});
