// ********RoostGPT********
/*
Test generated by RoostGPT for test checkNodejsDepsInstalled using AI Type Open AI and AI Model gpt-4


ROOST_TEST_HASH=printLine_adf0a41ef1

*/

// ********RoostGPT********
const expect = require('chai').expect;
const sinon = require('sinon');
const printLine = require('./app');

describe('printLine', function() {
  let consoleLogStub;

  beforeEach(function() {
    consoleLogStub = sinon.stub(console, 'log');
  });

  afterEach(function() {
    consoleLogStub.restore();
  });

  it('should print green text when required is true', function() {
    printLine(true, 'Test text');
    expect(consoleLogStub.calledOnce).to.be.true;
    expect(consoleLogStub.calledWith(sinon.match.string)).to.be.true;
    expect(consoleLogStub.getCall(0).args[0]).to.include('Test text');
  });

  it('should print red text when required is false', function() {
    printLine(false, 'Test text');
    expect(consoleLogStub.calledOnce).to.be.true;
    expect(consoleLogStub.calledWith(sinon.match.string)).to.be.true;
    expect(consoleLogStub.getCall(0).args[0]).to.include('Test text');
  });

  it('should throw error when text is not provided', function() {
    expect(() => printLine(true)).to.throw();
  });
});
