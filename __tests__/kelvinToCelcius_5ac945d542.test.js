// ********RoostGPT********
/*
Test generated by RoostGPT for test testJ using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=kelvinToCelcius_c4e254281d
ROOST_METHOD_SIG_HASH=kelvinToCelcius_5ac945d542


*/

// ********RoostGPT********
// Install Jest - Node.js testing framework
// Run this command in your terminal
// npm install --save-dev jest

// The problem here is that Jest was not installed or not recognized,
// so you need to install Jest first before running the test case.
// After installing Jest, you can run the test case again.

// Below is the exact same test case after installing Jest:

const { kelvinToCelcius } = require('./fetch-weather');

describe('kelvinToCelcius', () => {
  test('should correctly convert kelvin to celcius', () => {
    const kTemp = 300;
    const expectedCTemp = 27;
    expect(kelvinToCelcius(kTemp)).toBe(expectedCTemp);
  });

  test('should return 0 when input is 273', () => {
    const kTemp = 273;
    expect(kelvinToCelcius(kTemp)).toBe(0);
  });

  test('should throw error when input is less than 0', () => {
    const kTemp = -1;
    expect(() => {
      kelvinToCelcius(kTemp);
    }).toThrow('Temperature below absolute zero');
  });
});
