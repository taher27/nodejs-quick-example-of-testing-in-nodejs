// ********RoostGPT********
/*
Test generated by RoostGPT for test testing-nodejs using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=averageTemp_c5e4f830d6
ROOST_METHOD_SIG_HASH=averageTemp_db5e66b0a6


*/

// ********RoostGPT********
// First, import the necessary modules
const { averageTemp } = require('../weather/prepared-for-the-weather');

// Define the test suite for the 'averageTemp' function
describe('averageTemp', () => {

  // Test case 1: Check with valid numbers
  test('should return the average of two valid numbers', () => {
    const min = 2;
    const max = 4;
    const expected = 3;
    const result = averageTemp(min, max);
    expect(result).toBe(expected);
  });

  // Test case 2: Check with non-numbers
  test('should return NaN for non-number inputs', () => {
    const min = "test";
    const max = "test2";
    const result = averageTemp(min, max);
    expect(result).toBeNaN();
  });

  // Test case 3: Check with one number and one non-number
  test('should return NaN if one input is a number and the other is not', () => {
    const min = 2;
    const max = "test";
    const result = averageTemp(min, max);
    expect(result).toBeNaN();
  });

  // Test case 4: Check with negative numbers
  test('should return the average of two negative numbers', () => {
    const min = -2;
    const max = -4;
    const expected = -3;
    const result = averageTemp(min, max);
    expect(result).toBe(expected);
  });
});
