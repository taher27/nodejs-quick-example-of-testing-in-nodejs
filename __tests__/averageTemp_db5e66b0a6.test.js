// ********RoostGPT********
/*
Test generated by RoostGPT for test testing-nodejs using AI Type  and AI Model 

ROOST_METHOD_HASH=averageTemp_700473064c
ROOST_METHOD_SIG_HASH=averageTemp_db5e66b0a6


*/

// ********RoostGPT********
// Import the required packages
const { averageTemp } = require('./prepared-for-the-weather');

describe('averageTemp', () => {

  // Test case for valid numeric inputs
  test('should return average of two numbers', () => {
    const result = averageTemp(10, 20);
    expect(result).toBe(15);
  });

  // Test case for invalid inputs
  test('should return NaN for non-numeric inputs', () => {
    const result = averageTemp('10', '20');
    expect(result).toBe(NaN);
  });

  // Test case for null inputs
  test('should return NaN for null inputs', () => {
    const result = averageTemp(null, null);
    expect(result).toBe(NaN);
  });

  // Test case for undefined inputs
  test('should return NaN for undefined inputs', () => {
    const result = averageTemp(undefined, undefined);
    expect(result).toBe(NaN);
  });

  // Test case for mixed inputs
  test('should return NaN for one numeric and one non-numeric input', () => {
    const result = averageTemp(10, '20');
    expect(result).toBe(NaN);
  });

});
