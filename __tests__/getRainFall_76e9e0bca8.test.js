// ********RoostGPT********
/*
Test generated by RoostGPT for test testM using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=getRainFall_3237bb46a3
ROOST_METHOD_SIG_HASH=getRainFall_76e9e0bca8


*/

// ********RoostGPT********
const assert = require('assert');
const { getRainFall } = require('./fetch-weather');

describe('getRainFall', function() {
  it('should return 0 if rainObj is undefined', function() {
    const result = getRainFall(undefined);
    assert.equal(result, 0);
  });

  it('should return 0 if rainObj is null', function() {
    const result = getRainFall(null);
    assert.equal(result, 0);
  });

  it('should return "1h" rainfall if it exists', function() {
    const rainObj = { "1h": 10, "2h": 20, "3h": 30 };
    const result = getRainFall(rainObj);
    assert.equal(result, 10);
  });

  it('should return "2h" rainfall if "1h" does not exist', function() {
    const rainObj = { "2h": 20, "3h": 30 };
    const result = getRainFall(rainObj);
    assert.equal(result, 20);
  });

  it('should return "3h" rainfall if "1h" and "2h" do not exist', function() {
    const rainObj = { "3h": 30 };
    const result = getRainFall(rainObj);
    assert.equal(result, 30);
  });

  it('should return 0 if "1h", "2h" and "3h" do not exist', function() {
    const rainObj = { "4h": 40 };
    const result = getRainFall(rainObj);
    assert.equal(result, 0);
  });
});
