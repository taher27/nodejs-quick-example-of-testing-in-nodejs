// ********RoostGPT********
/*
Test generated by RoostGPT for test testM using AI Type Open AI and AI Model gpt-4

ROOST_METHOD_HASH=getRainFall_3237bb46a3
ROOST_METHOD_SIG_HASH=getRainFall_76e9e0bca8


*/

// ********RoostGPT********
const expect = require('chai').expect;
const fetchWeather = require('../weather/fetch-weather');

describe('fetchWeather', () => {
  describe('#getRainFall', () => {
    it('should return 0 if no parameter is passed', () => {
      const result = fetchWeather.getRainFall();
      expect(result).to.equal(0);
    });

    it('should return 0 if rainObj does not contain "1h", "2h", or "3h"', () => {
      const result = fetchWeather.getRainFall({ "4h": 10 });
      expect(result).to.equal(0);
    });

    it('should return the value of "1h" if it exists in rainObj', () => {
      const result = fetchWeather.getRainFall({ "1h": 10 });
      expect(result).to.equal(10);
    });

    it('should return the value of "2h" if it exists in rainObj and "1h" does not', () => {
      const result = fetchWeather.getRainFall({ "2h": 20 });
      expect(result).to.equal(20);
    });

    it('should return the value of "3h" if it exists in rainObj and neither "1h" nor "2h" does', () => {
      const result = fetchWeather.getRainFall({ "3h": 30 });
      expect(result).to.equal(30);
    });
  });
});
